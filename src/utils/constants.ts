// ─────────────────────────────────────────────────────────────
// Shared Constants & Defaults
// ─────────────────────────────────────────────────────────────

/** Default CSS font shorthand. */
export const DEFAULT_FONT = '16px sans-serif';

/** Default font size in CSS pixels. */
export const DEFAULT_FONT_SIZE = 16;

/** Default line height multiplier (applied to font size). */
export const DEFAULT_LINE_HEIGHT_MULTIPLIER = 1.5;

/** Default text color. */
export const DEFAULT_COLOR = '#000000';

/** Debounce interval for accessibility mirror updates during streaming (ms). */
export const A11Y_DEBOUNCE_MS = 300;

/** Default auto-scroll behavior. */
export const DEFAULT_AUTO_SCROLL = true;

/** Default aria-live mode. */
export const DEFAULT_ARIA_LIVE: 'polite' | 'assertive' | 'off' = 'polite';

/** Default white-space mode. */
export const DEFAULT_WHITE_SPACE: 'normal' | 'pre-wrap' = 'normal';
